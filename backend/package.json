{
  "name": "democracy-backend-minimal",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "migrate": "node --env-file=.env ./dist/src/index.js migrate",
    "ingest": "node --env-file=.env ./dist/src/index.js ingest",
    "dev:migrate": "tsx src/index.ts migrate",
    "dev:ingest": "tsx src/index.ts ingest"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "tsx": "^4.16.0",
    "typescript": "^5.6.3"
  }
}
